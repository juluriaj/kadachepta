{
  "name": "kathachepta",
  "version": "0.2.0",
  "private": true,
  "description": "KathaChepta: human-narrated stories and audiobooks for families",
  "scripts": {
    "setup": "python scripts/setup_local.py",
    "start": "docker compose up -d --build",
    "stop": "docker compose down",
    "status": "docker compose ps",
    "logs": "docker compose logs -f --tail 100 api worker-media",
    "test": "docker compose --profile test run --rm api-test",
    "users:demo": "docker compose exec api python -m app.cli seed-demo",
    "migrate:legacy": "docker compose exec api python -m app.migrate_sqlite",
    "worker:gpu": "powershell -ExecutionPolicy Bypass -File scripts/run_gpu_worker.ps1",
    "benchmark:llm": "python worker/bench/llm_benchmark.py --models qwen/qwen3-8b google/gemma-3-12b"
  }
}
